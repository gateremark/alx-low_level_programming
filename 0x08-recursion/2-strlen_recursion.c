#include "holberton.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: string
 * Return: length
*/

int _strlen_recursion(char *s)
{	int n;

	n = strlen(s);
	return (n);
}
