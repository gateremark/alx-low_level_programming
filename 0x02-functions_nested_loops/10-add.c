#include "main.h"
/**
 * add - a function that adds two integers
 * and return yhe result
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Add of number one and two.
 */
int add(int a, int b)
{
	return (a + b);
}
